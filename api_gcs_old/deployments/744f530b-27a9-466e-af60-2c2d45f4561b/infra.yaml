name: GCP Subnetwork
type: gcp/subnet

parameters:
  type: object
  properties:
    name:
      type: string
      title: Name
  required:
    - name

inputs:
  type: object
  properties:
    network:
      type: object
      properties:
        id:
          type: string

outputs:
  type: object
  properties:
    subnet:
      type: object
      properties:
        id:
          type: string
